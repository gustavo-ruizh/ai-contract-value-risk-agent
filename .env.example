# Required — Anthropic API key for LLM agents (intake, payment extraction, risk assessment)
ANTHROPIC_API_KEY=sk-ant-...

# Optional — enables RAG evidence retrieval (chunking, embedding, vector search)
# If omitted, the pipeline runs without grounded evidence passages.
OPENAI_API_KEY=sk-...
